define(["responsiveNav"], function($) {
	// jquery the jquery.responsive.slide plugins have been loaded.
	$(function() {
		// lauch the slideshow with captions
		var navigation = responsiveNav("#nav");
	});
});